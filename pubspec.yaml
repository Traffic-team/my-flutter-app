name: smspanel
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"  # اطمینان از سازگاری با نسخه جدید Dart SDK

dependencies:
  flutter:
    sdk: flutter

  # پکیج‌های اضافه شده
  http: ^0.13.3
  shared_preferences: ^2.0.6
  flutter_background_service: ^5.0.10
  android_alarm_manager_plus: ^4.0.4
  telephony: ^0.2.0  # نسخه سازگار با null-safety برای دریافت پیامک

  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  # می‌توانید تصاویر و فایل‌های استاتیک خود را اینجا اضافه کنید
  # assets:
  #   - images/
